<%= form_with(model: user_record) do |form| %>
  <% if user_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_record.errors.count, "error") %> prohibited this user_record from being saved:</h2>

      <ul>
        <% user_record.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :file %>
    <%= form.file_field :file %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
